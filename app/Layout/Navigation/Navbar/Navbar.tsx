// Add a simple Navbar component with a search bar and a button for the user to sign in
import { Button } from "@/components/ui/";

export default function Navbar() {
  return (
    <div>
      <Button>Course Centre</Button>
    </div>
  );
}
